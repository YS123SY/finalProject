import { ADD_ARTICALE, REMOVE_ARTICALE, UPDATE_ARTICALE } from "../actions/types";

const INITIAL_STATE = {
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
